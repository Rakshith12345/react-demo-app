apiVersion: v1
kind: Service
metadata:
  name: python-service
  namespace: react-app-namespace
spec:
  selector:
    app: python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
